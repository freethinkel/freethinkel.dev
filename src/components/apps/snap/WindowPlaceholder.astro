<div class="wrapper"></div>

<style>
  .wrapper {
    position: absolute;
    background: color-mix(in srgb, transparent, var(--color-accent) 20%);
    border-radius: 10px;
    border: 2px solid var(--color-accent);
    animation: animate 5s infinite cubic-bezier(0.1, 0, 0, 1);
    --padding: 10px;
    --statusbar-height: 31px;
  }

  @keyframes animate {
    0% {
      top: calc(var(--statusbar-height) + var(--padding));
      left: var(--padding);
      bottom: var(--padding);
      width: 50%;
    }

    20% {
      top: calc(var(--statusbar-height) + var(--padding) + 50%);
      left: var(--padding);
      bottom: calc(var(--padding));
      width: 50%;
    }

    40% {
      top: calc(var(--statusbar-height) + var(--padding) + 50%);
      left: calc(var(--padding) + 50%);
      bottom: calc(var(--padding));
      width: calc(50% - var(--padding) * 2);
    }

    60% {
      top: calc(var(--statusbar-height) + var(--padding));
      left: var(--padding);
      bottom: calc(var(--padding));
      width: 50%;
    }
    80% {
      top: calc(var(--statusbar-height) + var(--padding));
      left: var(--padding);
      bottom: var(--padding);
      width: calc(100% - var(--padding) * 2);
    }

    100% {
      top: calc(var(--statusbar-height) + var(--padding));
      left: var(--padding);
      bottom: var(--padding);
      width: 50%;
    }
  }
</style>
